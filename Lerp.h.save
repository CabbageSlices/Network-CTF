#ifndef LERP_H_INCLUDED
#define LERP_H_INCLUDED

//interpolate from the given value to the destination value and return how much the given value should increase by in order to reach the destination value
//that is, gives the distance from the origin value to the current value
///does not give the originValue + amountToMove, instead it just gives the amount to move so you'll have to add it yourself
const float interpolate(const float& originValue, const float& destinationValue, const float& deltaFraction);

#endif // LERP_H_INCLUDED
